The python codes are direct translations from the matlab codes and generate consistent results.
The variable names used in the python codes are the same as the matlab codes, and 
the results can be replicated as follows:

Some slight difference exists between the results from matlab and python exists 
due to their intrinsic noise functions (see the folder "py_codes/results").


### Figures 2–4 (General Instructions)

The results in Figures 2–4 can be replicated using:
- TFUS simulation: `main_FUS.py`
- TMS simulation: `main_TMS.py`

#### TFUS parameters
Set the following variables:

- Sonication duration: `params.Duration`
- Pulse repetition frequency (PRF): `params.stim.cont.prf_hz` 
- Amplitude: `params.stim.cont.mean`
- Duty cycle: `duty_cycle`

**Important:**
- TFUS amplitude is derived from TMS amplitude (`amp_TMS = 40`) via the equivalent-energy principle (Eq. 1)
- **Exception:** Figure 4a — replace the equation with explicit values

#### TMS parameters

- Duration: `params.Duration` 

Protocol configurations:

- **cTB-TMS**
params.bursts = 3
params.freq = 50
params.oscillation_freq = 5
params.on = 2
params.off = 0


- **iTB-TMS**
params.bursts = 3
params.freq = 50
params.oscillation_freq = 5
params.on = 2
params.off = 8

- **rTMS (1 Hz or 20 Hz)**

params.bursts = 1
params.freq = 50
params.oscillation_freq = 1 or 20
params.on = 2
params.off = 0


---

### Plasticity Output

Plasticity is quantified as:

> Ratio of MEP change relative to baseline

Output variable:

pct_change_nu_ee


---

### Simulation Timing

- Stimulation onset time: **50 s**
- Total simulation time:  T 

T = onset + stimulation duration + post-stimulation time


**Example:**
- cTB-TMS-40s with 10 min post-stimulation:

T = 50 + 40 + 600 = 690


---

## Figure 2

**b-c.**
- cTB-TMS-40s and cTB-TMS-80s  
- Post-stimulation time: 600 s

- cTB-TFUS-40s and cTB-TFUS-80s  
- PRF = 5 Hz  
- Duty cycle = 10%  
- Duration = 40 s / 80 s  
- Post-stimulation time: 300 s  

**d.**
- All TMS protocols  
- Post-stimulation time: 600 s  

**e.**
- All TFUS protocols  
- Duration = 80 s  
- Duty cycle = 10%  
- Post-stimulation time: 300 s  

---

## Figure 3

**c.**
- Omega function same as Figure 1  

**e.**
- Protocols same as Figure 2  

Variables correspond to:
- `nu_ee` → nu 
- `nutil_ee` → nu_tilda 
- `Ca_ee` → intracellular calcium concentration  

Locations:
- `main_FUS.py` 
- `main_TMS.py` 

---

## Figure 4 — TFUS Parameter Sweep (plasticity effect recorded at post-stim time at 5 min)

**a.** : run main_FUS_loop_amp.py
- Replace equation with explicit amplitude values  
- (Only case where TFUS amplitude is manually changed)  

Default:
- Duration = 80 s  
- PRF = 5 Hz  
- Duty cycle = 10%  



**b.** : run main_FUS_loop_duration.py
- PRF fixed at 5 Hz  
- Duty cycle fixed at 10%  

**c.**: run main_FUS_loop_PRF.py  
- Duration fixed at 80 s  
- Duty cycle fixed at 10%  

**d.**: run main_FUS_loop_DC.py
- Duration fixed at 80 s  
- PRF fixed at 5 Hz  

**e.**: run main_FUS_loop_DC_same_energy.py
- PRF fixed at 5 Hz  
- Duration adjusted based on duty cycle  

Constraint:

Total TFUS-ON time = duty cycle × duration = constant (10% × 80 s)

